volumes:
  iacpaas_materials_local_postgres_data: {}
  iacpaas_materials_local_postgres_data_backups: {}
  iacpaas_materials_local_redis_data: {}

services:
  django: &django
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: iacpaas_materials_local_django
    container_name: iacpaas_materials_local_django
    depends_on:
      - postgres
      - redis
    volumes:
      - /app/.venv
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8000:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: iacpaas_materials_production_postgres
    container_name: iacpaas_materials_local_postgres
    volumes:
      - iacpaas_materials_local_postgres_data:/var/lib/postgresql/data
      - iacpaas_materials_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres

  redis:
    image: docker.io/redis:8.4
    container_name: iacpaas_materials_local_redis
    volumes:
      - iacpaas_materials_local_redis_data:/data

  celeryworker:
    <<: *django
    image: iacpaas_materials_local_celeryworker
    container_name: iacpaas_materials_local_celeryworker
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celeryworker

  celerybeat:
    <<: *django
    image: iacpaas_materials_local_celerybeat
    container_name: iacpaas_materials_local_celerybeat
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celerybeat

  flower:
    <<: *django
    image: iacpaas_materials_local_flower
    container_name: iacpaas_materials_local_flower
    ports:
      - '5555:5555'
    command: /start-flower
